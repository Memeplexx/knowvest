# NOTE: Prisma needs this for local migrations. When using dot env in conjunction with .env.environment.local, Prisma refuses to skip seeding even though --skip-seed arg is passed.
POSTGRES_PRISMA_URL="postgresql://postgres:postgres@localhost:5432/knowvest?schema=public"